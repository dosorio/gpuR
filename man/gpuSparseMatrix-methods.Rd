% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpuSparseMatrix.R

\name{gpuSparseMatrix}
\alias{gpuSparseMatrix}
\alias{as.gpuSparseMatrix}
\title{Create GPU Sparse Matrix}
\usage{
gpuSparseMatrix(x, type = "double", ctx_id = 0L,
                platform_index = 0L, device_index = 0L,
                row_indices = NULL, col_indices = NULL,
                nrows = NULL, ncols = NULL)

as.gpuSparseMatrix(x, ctx_id = 0L, platform_index = 0L, device_index = 0L)
}
\arguments{
\item{x}{A sparse matrix (e.g., from \code{Matrix::sparseMatrix},
\code{Matrix::Matrix(sparse=TRUE)}, etc.) or a dense matrix to be sparsified,
or vectors for CSR construction: \code{values}, \code{row_indices}, \code{col_indices}}

\item{type}{A character string specifying the data type: "integer", "float", or "double"}

\item{ctx_id}{Integer index of the context on which to place the object}

\item{platform_index}{Integer index of the platform}

\item{device_index}{Integer index of the device}

\item{row_indices}{Vector of row indices for CSR construction (optional)}

\item{col_indices}{Vector of column indices for CSR construction (optional)}

\item{nrows}{Number of rows (required if constructing from vectors)}

\item{ncols}{Number of columns (required if constructing from vectors)}
}
\value{
An object of class \code{dgpuSparseMatrix}, \code{fgpuSparseMatrix},
or \code{igpuSparseMatrix} depending on the specified type.

For \code{as.gpuSparseMatrix}, returns an appropriate gpuSparseMatrix object
with automatically detected type.
}
\description{
Create a sparse matrix object for GPU computation using Compressed Sparse Row (CSR) format.

Convenience function to convert a matrix to a GPU sparse matrix with automatic type detection.
}
\details{
The function supports creation of sparse matrices in several ways:
\enumerate{
  \item From a sparse matrix object (Matrix package)
  \item From a dense matrix (will be automatically sparsified)
  \item From CSR components: values, row_indices, col_indices
}

Sparse matrices are stored in Compressed Sparse Row (CSR) format for optimal GPU performance.

Automatically detects the data type of the input matrix and creates the appropriate
GPU sparse matrix type.
}

